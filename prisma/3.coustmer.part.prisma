model Customer {
  id        String   @id @default(auto()) @map("_id") @db.ObjectId
  user      String   @db.ObjectId
  photo     String
  name      String
  address   String
  email     String
  phone     String
  notes     String
  workOrders Work_order[]
}

